FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# Run in docker mode to use correct backend URL
CMD ["npm", "run", "dev:docker", "--", "--host", "0.0.0.0"]
