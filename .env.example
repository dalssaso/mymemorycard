# Database (Docker PostgreSQL on port 5433)
# DB_PASSWORD is used by docker-compose to set the PostgreSQL password
DB_PASSWORD=your-db-password

# DATABASE_URL is used by the backend to connect - note the port 5433!
DATABASE_URL=postgresql://mymemorycard:your-db-password@localhost:5433/mymemorycard

# Redis (Docker Redis on port 6380)
REDIS_URL=redis://localhost:6380

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your-dev-jwt-secret-change-in-production

# RAWG API (get key from https://rawg.io/apidocs)
RAWG_API_KEY=

# IGDB API (optional, get from https://api-docs.igdb.com/)
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=

# HowLongToBeat (optional, for time estimates)
HLTB_COOKIE=
HLTB_USER_AGENT=

# Integration Testing (optional, for running tests)
# API_BASE_URL defaults to http://localhost:3000
API_BASE_URL=http://localhost:3000
# TEST_DATABASE_URL defaults to postgresql://mymemorycard:devpassword@localhost:5433/mymemorycard
TEST_DATABASE_URL=postgresql://mymemorycard:devpassword@localhost:5433/mymemorycard
