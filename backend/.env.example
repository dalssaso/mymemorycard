# Backend Environment Variables
# Copy to .env - ALL variables without defaults are REQUIRED

# === REQUIRED (server won't start without these) ===
DATABASE_URL=postgresql://mymemorycard:devpassword@localhost:5433/mymemorycard
REDIS_URL=redis://localhost:6380

# JWT_SECRET: >= 32 characters (min 32 in production). Must be changed for production!
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=dev-jwt-secret-change-in-production-32chars-minimum

# RAWG_API_KEY: Required for third-party game metadata integration
# Get from: https://rawg.io/api
# Leave empty to disable RAWG integration (graceful degradation)
RAWG_API_KEY=your_rawg_api_key_here

# ENCRYPTION_SECRET: >= 32 characters (min 32 in production). Must be changed for production!
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_SECRET=dev-encryption-secret-change-in-production-32chars-minimum

# ENCRYPTION_SALT: >= 16 characters (min 16 in production). Must be changed for production!
# Generate: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ENCRYPTION_SALT=dev-encryption-salt-change-in-production-16chars-minimum

# === OPTIONAL (have sensible defaults) ===
# PORT=3000
# ORIGIN=http://localhost:5173
# BCRYPT_SALT_ROUNDS=10
# JWT_EXPIRES_IN=7d
# DB_POOL_MAX=10
# DB_POOL_IDLE_TIMEOUT=30
# DB_POOL_CONNECT_TIMEOUT=10

# === TESTING ===
# SKIP_REDIS_CONNECT=1
